# --- DATABASE CONNECTION ---
# Used natively by psycopg and LangGraph Postgres Saver
DATABASE_URL=postgresql://user:password@localhost:5432/morning_briefing

# --- STREAMLIT CONNECTION POOL SETTINGS ---
# Keep MIN_SIZE roughly equal to your max expected concurrent users if possible
DB_POOL_MIN_SIZE=1 
DB_POOL_MAX_SIZE=20

MODEL_NAME=ollama:qwen3:8b
TAVILY_API_KEY=tvly-dev-xxxxxxxxxx